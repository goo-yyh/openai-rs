[package]
name = "realtime_consumer"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
openai-core = { path = "../../", default-features = false, features = ["realtime", "responses-ws", "rustls-tls"] }
