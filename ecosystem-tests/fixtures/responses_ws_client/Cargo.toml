[package]
name = "responses_ws_client"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
openai-core = { path = "../../../", default-features = false, features = ["responses-ws", "rustls-tls"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
