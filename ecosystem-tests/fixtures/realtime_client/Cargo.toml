[package]
name = "realtime_client"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
openai-rs = { path = "../../../", default-features = false, features = ["realtime", "rustls-tls"] }
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
